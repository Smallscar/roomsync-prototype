
import React from "react";
import ReactDOM from "react-dom/client";
import RoomSyncPrototype from "./App";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <RoomSyncPrototype />
  </React.StrictMode>
);
